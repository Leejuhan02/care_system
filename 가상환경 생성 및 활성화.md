가상환경 생성 및 활성화

/usr/local/bin/python3.11 -m venv /home/raspberry/my\_venv\_311

source /home/raspberry/my\_venv\_311/bin/activate

pip install --upgrade pip

cd /home/raspberry/care\_system

pip install -r requirements.txt





tflite 모델 모드

source /home/raspberry/my\_venv\_311/bin/activate

cd /home/raspberry/care\_system

python run\_app.py --mode tflite --model models/fall\_detection.tflite --camera 0 --display



tflite 런타인 설치 (휠)

pip install /path/to/tflite-runtime-<version>-cp311-cp311-linux\_aarch64.whl





권장: 프로젝트는 venv 바깥(예: /home/raspberry/care\_system)에 두고 venv는 /home/raspberry/my\_venv\_311로 둡니다.





가상환경 준비, 이미 있다면 건너뜀

\# (선택) Python 3.11 설치 (필요한 경우)

\# /usr/local/bin/python3.11 -m venv /home/raspberry/my\_venv\_311

/usr/local/bin/python3.11 -m venv /home/raspberry/my\_venv\_311

source /home/raspberry/my\_venv\_311/bin/activate

pip install --upgrade pip



의존성 준비

cd /home/raspberry/my\_venv\_311/care\_system   # 또는 /home/raspberry/care\_system

pip install -r require0ments.txt



TFLite를 쓸 경우: Raspberry Pi 5 + Python 3.11(aarch64/cp311)에 맞는 tflite-runtime 휠(.whl)을 찾아 직접 설치:

pip install /path/to/tflite-runtime-<version>-cp311-cp311-linux\_aarch64.whl



카메라 권한 준비 

sudo apt install -y v4l-utils

v4l2-ctl --list-devices

\# 권한 이슈면:

sudo usermod -a -G video $USER

\# 변경 후 재로그인 필요



tf모델 사용

source /home/raspberry/my\_venv\_311/bin/activate

cd /home/raspberry/my\_venv\_311/care\_system

python run\_app.py --mode tflite --model models/fall\_detection.tflite --camera 0 --display



sudo systemctl daemon-reload

sudo systemctl enable --now care\_system.service

sudo systemctl status care\_system.service





(테스트 권장 순서)

venv 활성화 → pip install -r requirements.txt

python \[run\_fall\_detection.py](http://\_vscodecontentref\_/2) --model \[fall\_detection.tflite](http://\_vscodecontentref\_/3) --camera 0 (모델이 있으면)

모델이 없으면 python \[run\_app.py](http://\_vscodecontentref\_/4) --mode heuristic --camera 0 --display로 카메라·알고리즘 정상 동작 확인

정상 동작 확인 후 main.py 또는 VideoProcessor에 통합/서비스화



